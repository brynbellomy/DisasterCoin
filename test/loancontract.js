/*
const LoanContract = artifacts.require("./LoanContract.sol");
const { expectThrow } = require("./LoanContract.js");

contract('Loan', (accounts) => {

  let loanContract;
  const loanOwner = accounts[0];

  beforeEach(() => {
    return LoanContract.new({from: loanOwner })
    .then(instance => {
      loanContract = instance
    });
  });

  it("Should be able to add a tag", async () => {

  });

  it("Should be able to add a tag to a vendor", async () => {

  });
});

*/
